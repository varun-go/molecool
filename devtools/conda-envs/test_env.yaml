name: test
channels:
dependencies:
    #list all the packages needed for our testing

    # Base depends
  - python
  - pip

  - numpy
  - matplotlib
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

